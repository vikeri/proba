# import ElixirScraper
# IO.puts(cleanedhtml(html))
# IO.inspect(filter_out(html, "script"))
# IO.inspect(filterer([{"body", [], [{"script", [], []},{"div", [], []}]}]))
# IO.puts("\n\n")
# url = "time.is"
# unless String.contains?(url, "http") do
# 	url = "http://" <> url
# end
# scrap = scraper(url, "#twd")
# IO.puts(scrap.())
# {h, m, s} = :calendar.local_time() |> elem(1)
# IO.puts("#{h}:#{m}:#{s}")

# sender(scrap, scrap.(), url)
#
IO.puts IO.ANSI.faint <> "hej"

System.cmd("printf", ["Hejs"])

#
